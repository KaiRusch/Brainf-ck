#include <stdio.h>

int main(int argc, char **argv)
{
  if(argc != 1)
    {
      return -1;
    }

  char *inputFileName = argv[1];

  FILE *inputFile = fopen(inputFileName, "r");
  FILE *outputFile = fopen("out.c","w");

  fprintf(outputFile,"#include<stdio.h>\nint main(){\n");


  fprintf(outputFile,"return 0;\n}");

  fclose(inputFile);
  fclose(outputFile);
  

  return 0;
}
